spring:
  application:
    name: mu
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${DB_HOST:localhost}:3306/mu
    username: root
    password: Thakur@7

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true
    show-sql: true
    properties:
      hibernate:
        format_sql: true

cloudinary:
  apiKey: 645868366775829
  apiSecret: ehq83EKh3G0S2ny8vLyQhsblRiI
  cloudName: dq6fhd4js